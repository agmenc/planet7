package planet7.tabular.rec

import org.scalatest.{MustMatchers, WordSpec}

class RecSpec extends WordSpec with MustMatchers {
  "We can rec files in two directories by their names" in {
    fail("Not yet we can't")
  }

  "We can rec files from any two input URIs by their names" in {
    fail("Not yet we can't")
  }
}